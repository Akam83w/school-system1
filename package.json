{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,

  "scripts": {
    "generate": "pnpm --filter @workspace/api-spec run codegen",

    "typecheck:libs": "tsc --build",

    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",

    "build": "pnpm run generate && pnpm run typecheck && pnpm -r --if-present run build",

    "db:push": "pnpm --filter @workspace/db run push",

    "start": "pnpm --filter @workspace/api-server run start",

    "dev": "pnpm -r run dev"
  },

  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
